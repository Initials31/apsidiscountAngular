import { DirectiveListeDirective } from './directive-liste.directive';

describe('DirectiveListeDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveListeDirective();
    expect(directive).toBeTruthy();
  });
});
